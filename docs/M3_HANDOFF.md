# M3 引き継ぎメモ

M2 完了後に M3 を進めるための、次セッション向けハンドオフ。
**M3 に関わる実装に入る前に必ず目を通す。** 進捗に合わせて更新していく。

最終更新: 2026-04-28（M3 #9 ディレクトリインスペクター landed → **M3 全フェーズ landing 完了**。残タスクは `tauri dev` 実機 smoke のみ）

---

## 1. 現在地

- M2 Naming rules engine + accepts 完了。`core::rules` / `core::accepts` / `core::naming` / `core::history` / `core::rename` / `core::sequence` / `core::sequence::drift` / `core::lint` 全コア + CLI `init`/`scan`/`doctor`/`clean`/`rename`/`lint`/`undo`/`redo` + history retention 50 が landed（IMPLEMENTATION_PLAN §0）。
- post-M2 リファクタ landed（PR #26）: CLI 共通化（`crate::output` / `crate::context` / `crate::walk`）、テストハーネス、`ApplyWarning` enum 統合。
- M3 スコープ整合（PR #27）: §0/§5/M2_HANDOFF が「M3 = import」と「M3 = 検索とビュー」で乖離していたのを §5 を正として揃え直し。**M3 = 検索とビュー**、**M4 = import + thumbnail + AI + template** が確定。
- M3 kickoff 議論で確定:
  - 順序: DSL 仕様書 → `core::search` → FTS5 → CLI search/tag → UI
  - PR 粒度: M2 同様フェーズ単位（7〜9 PR 想定）
  - スコープ追加: **Tauri IPC 層を M3 で同時に係る**、**lindera は v1.x defer を IMPLEMENTATION_PLAN に明示**
  - DSL 詰め方: M2 NAMING_RULES_DSL と同粒度の `docs/SEARCH_DSL.md` を最初に landed
- DSL 仕様書 [`docs/SEARCH_DSL.md`](./SEARCH_DSL.md) landed（feat/m3-search-dsl-spec）: 文法 EBNF / 予約キー全 8 種 / 自由テキスト FTS5 trigram / カスタムフィールド / 性能契約 (10k=50ms / 100k=100ms p95) / Worked examples 8 / v1.x defer 候補 / 実装メモ。lindera は §3.2 / §15 で v1.x defer を明示。
- `core::search` parser + AST + validate + planner landed（feat/m3-core-search-parser）: `progest_core::search::{ast, lex, parse, validate, plan, mod}`。二状態（Expr/Value）字句解析 + 再帰下降パーサ（OR > AND > NOT、`-` 単項、`( )` グループ、改行禁止）+ `Warning` 列挙 + `AlwaysFalse` 短絡 + `BindValue` パラメータ化 SQL 出力。`chrono` を workspace + core 依存に追加（datetime parse + UTC 正規化）。unit 72 + golden 8（§10 worked examples 1:1）= 全 80 テスト pass。
- `core::index` migration 0002 + `core::search::execute` landed（feat/m3-search-executor）: `files` に search-derivable 列（`name` / `ext` / `notes` / `updated_at` / `is_orphan`）追加、`custom_fields(file_id, key, value_text, value_int)` / `violations(file_id, category, severity, rule_id, message)` テーブル新設、`files_fts` FTS5 virtual table（`tokenize='trigram'`、name+notes、UPDATE/DELETE トリガで sync）。`SearchHit { file_id, path }` の薄い結果型 + `BindValue: ToSql`、planner SQL を outer SELECT で wrap して path 投影。`is:violation` / `is:misplaced` は violations テーブル、`is:duplicate` は fingerprint self-join、`is:orphan` は flag column 経由に変更（旧 `has_*_violation` 案を取りやめ）。22 statement-level integration test（empty DB / tag / type / and / or / not / freetext / FTS triggers / range / is: 全種 / glob / custom int / 順序 / 複合）+ 既存 80 search test = 全 102 search test pass。`progest-core` 全 561 + golden 8 + integration 全 pass。reconcile/lint hook で実列を埋める作業は M3 #5 CLI 着手時に合流。
- shadcn/ui 初期化 landed（feat/m3-shadcn-init）: `app/` workspace に Tailwind v4 (`tailwindcss` + `@tailwindcss/vite`) を導入、`@/*` import alias を `app/tsconfig.json`（baseUrl + paths）と `app/vite.config.ts`（`resolve.alias`）の両方に配線。`pnpm dlx shadcn@latest init --preset b1D0dy4m --template vite --pointer -c app` で `app/components.json` 生成（style=`radix-mira`、iconLibrary=`lucide`、baseColor=`neutral`、cssVariables=true、aliases は `@/components` / `@/components/ui` / `@/lib` / `@/lib/utils` / `@/hooks`）。`src/index.css` は `@theme inline` ブロック + light/dark トークン (oklch) + Geist Mono + `tw-animate-css` で初期化、`html { font-mono }` / `cursor: pointer for buttons`（`--pointer`）込み。`Button` / `Dialog` / `Command`（+ `Input` / `Textarea` / `InputGroup` を依存として）を `src/components/ui/` に追加、`@/lib/utils` 新設。`pnpm --filter @progest/app typecheck` + `build` + `mise run check` 全 green。コマンドパレット UI 配線（M3 #7）は別 PR。
- コマンドパレット UI + Tauri search IPC landed（feat/m3-command-palette）:
  - `core::search::history` 新設（`.progest/local/search-history.json`、`schema_version: 1` + `entries[]: { query, ts: DateTime<Utc> }`、`MAX_ENTRIES = 100`、`append` は dedup + 先頭挿入 + 切り詰め、空文字列 no-op、`clear` は entries のみ空に。`load`/`save` は `FileSystem` trait + atomic write、missing → `HistoryError::NotFound`。`ProjectRoot::search_history_json()` accessor + `SEARCH_HISTORY_FILENAME` const）。10 unit test（round-trip / 順序 / dedup / empty / retention / clear / schema mismatch / malformed JSON / atomic 上書き）pass。
  - `progest-tauri` IPC 4 commands: `app_info()` / `search_execute(query)` / `search_history_list()` / `search_history_clear()`。`AppState { project: Mutex<Option<ProjectContext>> }` に `ProjectRoot + StdFileSystem + SqliteIndex` を保持し、起動時 `discover_initial_project`（`PROGEST_PROJECT` env → CWD walk → None、フォールバック失敗でも shell は起動）で attach。`SearchResponse { query, hits, warnings, parse_error }` で parse 失敗を構造化（exit code でなく `parse_error.column` 付き）、validate warnings は string 化、custom_fields は `serde(tag="type", content="value")` でフロントへ。schema.toml ローダは CLI と同じ tiny TOML reader を inline。エラー文字列は `no_project:` プレフィックスでフロント側 `IpcError.isNoProject` 判定。
  - `app/` 側は `@/lib/ipc.ts`（typed wrappers + `IpcError`）/ `@/components/command-palette.tsx`（window keydown で Cmd+K / Ctrl+K toggle、`CommandDialog` + `shouldFilter={false}`、空入力で recent history を `search_history_list` から表示し「Clear recent queries」アクション付き、非空入力は **200 ms debounce** で `search_execute`、結果行に violations バッジ（naming amber / placement sky / sequence violet）、validate warnings + project name + IPC error は status row に集約、parse error はインライン destructive 表示、no-project は専用 empty state）/ `@/components/result-detail-dialog.tsx`（クリックで開く詳細 Dialog、path / file_id / name / ext / tags / violations / custom_fields をフラットに表示、選択時はパレットを閉じてモーダル重なりを避ける）。`App.tsx` は M0 placeholder を撤去し `<CommandPalette />` + Cmd+K hint。
  - `mise run check` + `pnpm --filter @progest/app build` green。**UI 動作確認は `tauri dev` + 実プロジェクトでの目視が未実施** — 自動 typecheck/build/clippy が通っているのみ、行動レベルの確認は次セッションで CLAUDE.md §作業パターン4 に沿って行う。
  - スコープ外（M3 #8 / #9 で合流予定）: tree/flat view、`view.{list,save,delete}` IPC、`accepts.{read,write}` IPC、placement バッジの正式色、ファイル `Reveal in Finder` などの hit クリック後アクション。

---

## 2. 着手順序（推奨）

| # | モジュール | 依存 | メモ |
| --- | --- | --- | --- |
| 1 | `docs/SEARCH_DSL.md` | なし | **landed**（feat/m3-search-dsl-spec、PR #28） |
| 2 | `core::search` parser + AST + validate + planner | DSL 仕様書 | **landed**（feat/m3-core-search-parser）。lex / parse / validate / plan の純関数 4 段。`AlwaysFalse` 短絡 + `Warning` 列挙、`BindValue` 化 SQL 出力。72 unit + 8 golden test。chrono 依存追加。 |
| 3 | `core::search` executor | planner + index migration | **landed**（feat/m3-search-executor、本 PR と一括）。`SearchHit { file_id, path }` の薄い結果型、planner SQL を outer SELECT で wrap して path 投影。`is:violation`/`is:misplaced`/`is:duplicate`/`is:orphan` を violations テーブル / fingerprint self-join / `is_orphan` 列で実装。 |
| 4 | `core::index` migration 0002 (search) | M1 `core::index` | **landed**（feat/m3-search-executor、本 PR と一括）。`files` に `name`/`ext`/`notes`/`updated_at`/`is_orphan` 列追加 + `custom_fields` / `violations` テーブル + `files_fts` FTS5 virtual table（trigram、INSERT/UPDATE/DELETE トリガで sync）。reconcile/lint からの書き手は M3 #5 で合流。 |
| 5 | CLI `progest search` / `progest tag` / `progest view` + reconcile/lint hooks | search executor + custom_fields | **landed**（feat/m3-cli-search-tag-view、本 PR）。`progest search <query>` (parse → validate → plan → execute → project rich) / `progest tag {add\|remove\|list}` / `progest view {save\|delete\|list}`。`Index::set_search_projection` / `replace_violations` / `set_custom_field` / `rich_rows` を追加、reconcile が name/ext/notes/updated_at を populate、lint が visited file_id ごとに violations を再構築。`core::tag` / `core::search::views` を新設、`SqliteIndex::with_connection` を pub に、`OutputFormat: Copy`、8 CLI smoke test。 |
| 6 | shadcn/ui 初期化 | Vite + React 19 が居る | **landed**（feat/m3-shadcn-init）。`app/` で Tailwind v4 + `@/*` alias 配線 → `pnpm dlx shadcn@latest init --preset b1D0dy4m --template vite --pointer -c app` で `components.json` 生成、`Button` / `Dialog` / `Command` 追加（`Input` / `Textarea` / `InputGroup` も依存で同時）。typecheck/build/mise check green |
| 7 | コマンドパレット UI + Tauri search IPC | shadcn / `core::search::execute` | **landed**（feat/m3-command-palette）。`core::search::history`（retention 100、dedup append）、Tauri 4 commands（`app_info` / `search_execute` / `search_history_list` / `search_history_clear`、`PROGEST_PROJECT` / CWD で起動時 attach）、`@/lib/ipc.ts` typed wrappers + `IpcError`、`command-palette.tsx`（Cmd+K toggle、200 ms debounce、空入力で recent history、violations バッジ、status row）、`result-detail-dialog.tsx`（クリックで全フィールド表示）。typecheck + build + `mise run check` green。`tauri dev` での実機目視は次セッション持ち越し。 |
| 8 | tree view + flat view（list / grid）+ saved-views UI + `view.*` / `files_list_dir` IPC | コマンドパレット + `core::search::views` | **landed**（feat/m3-tree-flat-views）。`View.display: ViewDisplay { List, Grid }`（serde default、欠落キーは list フォールバック）+ `progest view save --display`、Tauri `view_list/save/delete` + `files_list_dir(path)`（`IgnoreRules` + dotfile 隠し + `..` エスケープガード + index 補完）、`app/` は `<ProjectProvider>` + `<TreeView>`（lazy load）+ `<FlatView>`（saved view selector + list/grid toggle + Save-as ダイアログ + 200ms debounce）+ `<TopBar>`、`<ViolationBadges>` / `<ViolationDots>` 共有抽出。`mise run check` + `pnpm build` green。`tauri dev` 目視 smoke 持ち越し |
| 8.1 | M3 polish ラウンド（PR #36〜#41） | #6〜#8 全部 | **landed**。theme toggle (next-themes)、検索 DSL `type:a,b` コンマリスト + `type::alias` 展開、空クエリで `files_list_all` 全件 name-sorted、`.meta` / `.dirmeta.toml` の scanner / IPC defense フィルタ、project 切替時の FlatView+TreeView refresh、`>command` パレットモード（拡張可能 `palette-commands/` レジストリ + [`docs/COMMAND_PALETTE.md`](./COMMAND_PALETTE.md)）、`<StatusBar>`（違反バッジ集約 + ホバーで対象ファイル一覧 via shadcn Tooltip + project info + active view）+ FlatView Input に X クリアボタン + Esc キーハンドラ。詳細は §5 履歴を参照 |
| 9 | ディレクトリインスペクター + `accepts.{read,write}` + `lint_run` IPC | tree/flat view | **landed**（feat/m3-directory-inspector）。`core::accepts::writer`（`inject_accepts` / `remove_accepts`、token 順序保持、round-trip 7 test）。Tauri `accepts_read`（own + effective + chain + alias catalog + warnings、tagged-union token wire）、`accepts_write`（mode/alias 検証 + `expand_own_accepts` で write 前 unknown-alias 弾き）、`lint_run`（rules.toml/schema.toml/project.toml [cleanup] を inline ロード → `Index::list_files` → `lint_paths` → `write_to_index`）。`<DirectoryInspector>` で own/effective/chain/alias プレビュー + chip 入力 (`.ext` / `:alias` を inline parse、`""` で no-extension) + inherit checkbox + mode セレクタ + alias 展開ドロワー、保存後に `lintRun` を自動実行 → footer に `scanned/naming/placement/sequence` カウント表示 + `bumpRefresh()` で FlatView/TreeView を再 fetch。`<TreeView>` でディレクトリクリック = toggle + select（`bg-accent` ハイライト）。`App.tsx` を `<ResizablePanelGroup>` (Tree 22 / Flat 40 / Inspector 38、id 永続化) に置換。placement バッジ色は PR #36 の `--badge-placement` semantic token を流用。`mise run check` + `pnpm build` green。`tauri dev` 実機 smoke は次セッション。 |

各 PR で `progest-tauri` の IPC コマンド（search / tag / view CRUD / accepts edit）も合流させる。

---

## 3. ユーザーが「丁寧に見たい」と事前指定した領域（focus areas）

M3 着手中の節目（§2 #2, #3, #4, #6, #9）で AskUserQuestion で個別に詰める。

### 3.1 DSL 構文（確定済み）

[`docs/SEARCH_DSL.md`](./SEARCH_DSL.md) で以下を規定済み。parser 着手時は該当章を参照:

- §2 文法 EBNF（NOT > AND > OR、`OR` キーワード大文字、`-` 単項否定、`( )` グループ、改行禁止）
- §3 自由テキスト（FTS5 trigram、`name` + `notes` 列）
- §4 予約キー（tag/type/kind/is/name/path/created/updated）と多重 / 否定 / 範囲の意味論
- §6 カスタムフィールド（schema.toml 参照、未定義キーは parse OK + warning + 0 件）
- §7 query plan（AST → SQL の純関数、決定的、ゴールデンテスト可能）
- §8 結果スキーマ（CLI text / json）
- §9 エラー（parse error は exit 2、unknown_key は warning 集約）
- §10 Worked examples（golden fixture と 1:1 対応想定）

v1.x に送った項目（§15）: `sort:` / `limit:` / `parent:` `children:` / 近接検索 / facet / ranking / lindera / `extends` / relative date / `view:<id>` 展開。

### 3.2 FTS5 + trigram の動作確認

- `sqlite3` の `--features bundled` で FTS5 が確実に効く環境を確認（M1 で前提済み、改めて手動確認推奨）
- 短語（< 3 文字）の literal フォールバック挙動を golden test で固定
- CJK でのトリグラム生成境界（半角 / 全角混在、結合文字、絵文字）の corner case を CLI smoke で 1 件以上カバー

### 3.3 ビュースコープ（GUI / CLI 役割分担）

- `views.toml` の I/O は core で（read/write 両方）。CLI と Tauri は薄い
- `views.toml` を編集できる窓口:
  - CLI `progest view save <id> --query <q>` / `progest view delete <id>` / `progest view list`（M3 同時実装）
  - GUI 「ビューを保存」ダイアログ → IPC で core に渡す
- `local/history.json` も同様に core で I/O。retention 100 を core 側で担保

### 3.4 Tauri IPC の境界（M3 で確定させたい）

- 検索: `search.execute(query: string, view_id?: string) -> SearchResult`
- 検索履歴: `search.history.list() -> [HistoryEntry]` / `search.history.clear()`
- 保存ビュー: `view.list()` / `view.save(view: View)` / `view.delete(id)`
- ファイル一覧（tree / flat）: `files.list_tree(path)` / `files.list_flat(query)` — 後者は search.execute と統一できるか kickoff 時に再検討
- accepts 編集: `accepts.read(dir)` / `accepts.write(dir, accepts)` — `.dirmeta.toml` の書込は M2 既存 API を経由
- IPC 型は `progest-tauri/src/commands/*.rs` で `serde::Serialize` ↔ `core::*` の wire 構造を直接共有（M2 lint / clean / rename と同じ pattern）

### 3.5 shadcn 初期化の確認事項

- `--preset b1D0dy4m` の中身（カラーパレット / フォント等）が今のプロジェクトデザインと整合するか
- `--template vite` で `app/` の Vite 設定と衝突しないか（`vite.config.ts` の merge 戦略）
- `components.json` を `app/` 配下に置くか / リポジトリルートに置くか

shadcn skill が利用可能なので、init 着手時は `Skill: shadcn` を呼んでガイドに沿って進める。

### 3.6 placement バッジの色 / 位置

- naming 違反は M2 で既に lint UI（CLI text）に色付き、UI 側ではまだ未提示
- placement 違反バッジは naming とは別色（要件書 §3.13.6 のバッジ仕様を実装時に再確認）
- アイコン / 色は M5 アイコン差し替え時にも整合する規則で（仮: naming = amber、placement = sky、sequence = violet）

---

## 4. 横断的に忘れてはいけないこと

- **ドキュメント更新**: 各 PR 完了時に `docs/IMPLEMENTATION_PLAN.md §0` の進捗スナップショット、本ドキュメント（M3_HANDOFF）の履歴、新しく見つけた落とし穴は `docs/LESSONS.md` に追記、古くなった記述の削除まで含めてセット。
- **`docs/IMPLEMENTATION_PLAN.md` の M3 完了条件**: 「UI で `tag:foo type:psd is:violation` / `is:misplaced` 相当が 100ms 以下で返る、保存済みビューが永続化される、ディレクトリインスペクターで accepts を編集して `.dirmeta.toml` に反映される」（§5 M3）。M3 終わりに改めて確認。
- **性能ベンチ**: `docs/SEARCH_DSL.md §13` の規模・p95 目標を `tests/bench/search_smoke.rs` に固定。CI gate にはしない（参考値）。
- **破壊的操作**: search は read-only なので破壊的ではないが、`view save` / `view delete` / accepts 編集は preview → confirm を踏襲。
- **Tauri IPC は M3 で wire を確定**: M4 import / M5 thumbnail で IPC 表面が増える前に、search/view/accepts の IPC pattern を kickoff 時に固定する。
- **progest-merge**（`.meta` 用 git merge driver）は M3 範疇ではない（M4）。
- **lindera defer**: REQUIREMENTS §3.7 に既記載だが IMPLEMENTATION_PLAN §5 M3 にも明示で書く（PR #27 同時更新）。

---

## 5. 履歴

- 2026-04-28: M3 #9 landed（feat/m3-directory-inspector）。3 commit 構成: (1) `core::accepts::writer` 新設 — `inject_accepts(extra: &mut Table, accepts: &RawAccepts)` / `remove_accepts(extra: &mut Table)`、token 順序保持（chip 入力の見え方が source of truth）、`:alias` / `.ext` / `""` を loader と対称な形でシリアライズ。`extract_accepts` ↔ `inject_accepts` round-trip + 既存 dirmeta key（`tags`/`notes`/`custom`/`meta_internal`）が破壊されない 7 unit test。(2) `progest-tauri::accepts_commands` 新設 — `accepts_read(dir)` で own + effective（own/inherited 区別） + chain + alias catalog + warnings を一括返却、`accepts_write(dir, accepts: Option)` は mode 文字列 / alias 名 regex / `expand_own_accepts` での unknown-alias を write 前に検証。tagged-union JSON (`{type:"alias"|"ext"}`) でフロントが pattern match で分岐できる。`commands::no_project_error` / `load_alias_catalog_for_ctx` を `pub(crate)` に。`progest-tauri::lint_commands::lint_run` 新設 — CLI lint と同じ蓋を inline 化（`load_ruleset` / `load_alias_catalog_for_ctx` / `load_cleanup` を当 crate 内）、`Index::list_files` で path 集合を取って `lint_paths` → `write_to_index` を 1 round-trip。`Result<LintRunResponse { naming, placement, sequence, scanned }, String>`。accepts 7 unit test + 既存 + clippy + fmt all green。(3) `app/` 大幅再構成: `pnpm dlx shadcn@latest add resizable` で `react-resizable-panels` v4 を導入、`<ResizableHandle withHandle>` を含む 3-pane (Tree 22% / Flat 40% / Inspector 38%、id `progest:main-shell` で localStorage 永続化、orientation/id プロップ名は v4 仕様)。`<DirectoryInspector>`（own/effective/chain/alias プレビュー + chip エディタ + inherit + mode + alias 展開ドロワー + 保存後の lint refresh + footer に re-lint stats）、`<TreeView>` がディレクトリ選択を支援（クリックで toggle+select、`bg-accent` ハイライト）、`App.tsx` で `selectedDir` を Shell 状態に lift。`ProjectContext` に `refreshTick` + `bumpRefresh()` を追加し、FlatView の search effect deps と TreeView 専用の visited path 一括 refetch effect で連動（accepts 編集 → lintRun 成功 → bumpRefresh → 両 view が新 violations を表示）。注意点: shadcn の resizable wrapper が v4 の `react-resizable-panels` を使うため、`direction` プロップは `orientation` に変わっている / `autoSaveId` は `id` に統合されている（旧 v0/v1 / v2 と非互換）。`vite.config.ts` の `fmt.ignorePatterns` に `vite.config.{js,d.ts}` を追加（`tsc -b` 実行後に残る gitignored 副産物が `vp fmt --check` で fail しないよう defense）。`mise run check` + `pnpm --filter @progest/app build`（397 KB / 127 KB gzip、Resizable + Inspector 込みで polish 末から +1 KB 弱）green。**未実施**: `tauri dev` 実機 smoke（Cmd+K / accepts 編集 → 保存 → flat/tree 違反バッジが新 dirmeta を反映 / `is:misplaced` 動作 / 3-pane drag）は次セッションで CLAUDE.md §作業パターン4 に沿って行う。
- 2026-04-27: frontend toolchain を Vite+ に統合（chore/m3-app-vite-plus）。`app/` の lint / format が ESLint / Prettier 不在のままだったので、Evan You が率いる VoidZero の Vite+ ([viteplus.dev](https://viteplus.dev)、MIT) に乗せて oxlint + oxfmt + tsgo + Rolldown ベースの Vite を 1 ツールチェーンに統合。devDep `vite-plus` + `@voidzero-dev/vite-plus-core` を `app/` に追加、`pnpm-workspace.yaml` overrides で `vite` / `vitest` のトランジティブ参照も Vite+ core/test 経由に揃える（peer warning は alias 由来で実害なし）。`app/vite.config.ts` は `defineConfig` を `vite-plus` から import（こちらの module augmentation で `fmt` / `lint` キーが UserConfig に生える）し ESM 化のため `__dirname` → `import.meta.dirname`、`fmt: { ignorePatterns: ["dist/**", "src/components/ui/**"] }` + `lint: { options: {} }` を直書き。lint のルール本体は `app/.oxlintrc.json`（`categories: correctness=error / suspicious=warn / perf=warn`、`ignorePatterns: ["dist/**", "src/components/ui/**"]`）。`app/package.json` scripts は `dev/build/preview` を `vp` に切替、新規 `lint` (`vp lint`) / `fmt` (`vp fmt`) / `fmt:check` (`vp fmt --check`) / `check` (`vp check && tsc --noEmit`、`vp check` は fmt+lint のみで型は別途必要)。`mise run check` の `pnpm --filter @progest/app typecheck` を `... check` に置換、`mise run fmt` に `pnpm --filter @progest/app fmt` を追加。lefthook pre-commit に `oxfmt`（glob `app/**/*.{ts,tsx,js,jsx,json,css,md,html}` で `pnpm --filter @progest/app exec vp fmt --check`）を rustfmt の隣に追加（lint は pre-push の `mise run check` 経由でカバー、pre-commit は cheap に保つ）。CI は `mise run check` を呼ぶだけなので workflow 無変更で自動対応。既存 `src/` には `vp fmt` を一括適用 + lint は 0 warnings 0 errors（27 ファイル fmt / 19 ファイル lint）。`mise run check` 全体 5.78s green、`pnpm --filter @progest/app build` も green（バンドルサイズ 352 KB / 113 KB gzip で polish ラウンド時から +1 KB 弱、ビルドプラグインのバージョン差程度）。warning だけ残: `@vitejs/plugin-react` で `@vitejs/plugin-react-oxc` への移行が推奨されている — 別 issue で management（M3 #9 か後続フォローアップで対応可）。注意点: `vp lint --init` は `lint.options.ignorePatterns` を valid キーと誤って解釈するので（実際は `typeAware`/`denyWarnings`/`maxWarnings` 等のみ）、ignore は `.oxlintrc.json` 側で書く必要がある。`tsconfig.json` の `baseUrl` のせいで oxlint type-aware 機能は disabled（warning は出る、機能オフで動作する）— `npx @andrewbranch/ts5to6 --fixBaseUrl .` で型解決を `paths` ベースに移せるが M3 #9 以降に延期。
- 2026-04-27: M3 polish PR #41 landed（feat/status-bar）。画面下部に `<StatusBar>` 新設: 左に違反バッジ集約（naming/placement/sequence、ホバーで対象ファイルパス一覧を shadcn Tooltip で表示、retention 25 + `+N more`）、中央に project 名 + root（`min-w-0 shrink truncate`）、右に active saved view chip。`@/lib/flat-view-context.tsx` で `<FlatViewSummaryProvider>` + `useFlatViewSummary` / `useReportFlatView` を新設、`<FlatView>` から `{ activeView, violationTotals, violationFiles }` を `useEffect` で push、`<StatusBar>` は passive consumer。FlatView header の status row（searching / parse error / validate warnings / IPC error / hit count）は input 直下を維持（per-query feedback は input 近傍に置くべき、というユーザー方針）。FlatView Input に末尾 `<X>` クリアボタン + Esc キーハンドラ追加（active view からの decouple 込み、focused & non-empty 時のみで Dialog の Esc-to-close と競合せず）。`<ViolationBadges>` を `titles?: { naming?, placement?, sequence? }` 受付に拡張、`<Badge>` ラッパーで `titles` 提供時のみ shadcn Tooltip にラップ（密集リストでの portal コスト回避）。shadcn Tooltip primitive を `pnpm dlx shadcn@latest add tooltip` で導入、`<TooltipProvider delayDuration={150}>` を `App.tsx` 直下に配置。Welcome 画面も `grid-rows-[1fr_auto]` で StatusBar を常表示。バンドル 356 KB / 112 KB gzip。
- 2026-04-27: M3 polish PR #40 landed（feat/palette-system-commands）。コマンドパレットに VS Code 風 `>command` モード追加: `query.startsWith(">")` で `<CommandsBody>` に切替、検索 effect スキップ、no-project でも theme コマンド到達可、CommandInput の disabled 解除。拡張可能レジストリ設計: `app/src/lib/palette-commands/` に `PaletteCommand { id, title, group?, hint?, keywords?, run }` interface + AND-match `fuzzyMatch` を置き、source は hook 単位（`useProjectCommands` = Open project + Open recent: <name>、`useThemeCommands` = Set theme: System/Light/Dark）、`usePaletteCommands` aggregator で `useMemo` concat。新カテゴリ追加 = 新 hook 作成 → aggregator concat → `docs/COMMAND_PALETTE.md §1` テーブル追記の 3 ステップ。Long path hint は `min-w-0 max-w-[55%] truncate` + `title=` で 1 行固定 + ホバーで full path。新規ドキュメント [`docs/COMMAND_PALETTE.md`](./COMMAND_PALETTE.md) を canonical command list として整備。
- 2026-04-27: M3 polish PR #39 landed（fix/flatview-refresh-on-project-change）。`<FlatView>` と `<TreeView>` が project 切替時に古いプロジェクトの状態を保持していたバグ修正。両者で `useProject()` を subscribe、`project?.root` 変化時に reset: FlatView は query / response / error / activeView / display の初期化 + `viewList()` 再取得、TreeView は cache 全消し + expanded を root のみに戻し + root 再 fetch。FlatView の検索 effect deps に `project?.root` を追加して同一の空クエリでも別プロジェクトで再実行。
- 2026-04-27: M3 polish PR #38 landed（feat/empty-query-all-and-meta-filter）。空クエリ時に全ファイル name-sorted 表示 + project plumbing files の除外。新 IPC `files_list_all() -> Vec<RichSearchHit>` で `SELECT file_id, path FROM files ORDER BY LOWER(COALESCE(name, path)), path, file_id` → `project_hits` 拡張（rusqlite エラーは closure 境界で String 化して tauri crate に rusqlite 直接依存追加せず）、`<FlatView>` 空入力 branch で fetch + 既存 list/grid renderer に流し、cancellation flag で stale response 防止。`build_dir_entry` に `is_sidecar_filename(name)` 追加（`.meta` extension の case-insensitive + `.meta.tmp` literal suffix）、scanner `DEFAULT_PATTERNS` に `.dirmeta.toml` 追加（`<asset>.meta` は既存 `core::reconcile::is_sidecar` で処理済）、`files_list_all` / `search_execute` の rich 結果を `is_plumbing_path` で post-filter（古い build で indexed された stale 行のための defense-in-depth、reconcile 再実行で本来は drop される）。
- 2026-04-27: M3 polish PR #37 landed（feat/search-dsl-multi-and-alias）。検索 DSL にコンマリスト + `::alias` 展開を追加。適用キー: `tag:` / `type:` / `kind:` / カスタム値（quoted 値はリテラルで split 抑制）。`type::image` は `core::accepts::AliasCatalog`（builtin + project `schema.toml [alias]`）を引いて `(png OR jpg OR psd OR ...)` に展開、他キーへの `::name` は `unsupported_alias` warning + AlwaysFalse、不明 alias は `unknown_alias` warning。新 entry point `validate_with_catalog(query, schema, aliases)`、旧 `validate(query, schema)` は builtin alias のラッパー、CLI / Tauri caller は project の AliasCatalog を load して渡す。実装は validate 層のみで AST / lex / planner は不変（list 展開 = 内部で `Or` of N atoms 合成、planner は OR EXISTS で対応）、type counter は `validate_clause` に hoist して 1 list = 1 clause としてカウント（既存 `type-and-multi` warning 互換）。validate 11 unit test 追加（list / alias / unknown / unsupported / empty / quoted no-split / int range list）、`docs/SEARCH_DSL.md §4.4 / §4.10 / §4.11 / §9.2` に同期更新。
- 2026-04-27: M3 polish PR #36 landed（feat/app-theme-toggle）。`next-themes` で system / light / dark テーマトグル、`progest:theme` localStorage、`<ThemeToggle>` を TopBar / Welcome 右上に配置（lucide Sun/Moon/Monitor アイコン cycle、`Button variant="ghost" size="icon-sm"`）、`index.html` インラインスクリプトで FOUC 防止（同 storageKey）。shadcn `customization.md §Dark Mode` の公式推奨パターンに準拠（手書き `<ThemeProvider>` から差替）。`rules/styling.md` の「No manual `dark:` color overrides」も同 PR で対応: `--badge-naming` / `--badge-placement` / `--badge-sequence` / `--warning` を `:root` + `.dark` に semantic token として定義、`@theme inline` 経由で Tailwind utility 化（`text-badge-naming` / `bg-badge-naming/15` / `text-warning`）、`violation-badges.tsx` + `command-palette.tsx` + `flat-view.tsx` の `text-amber-600 dark:text-amber-300` 系を全て semantic token 参照に置換。`grep 'dark:' app/src/ | grep -v components/ui/` で 0 件確認（registry 生成コンポーネント以外で違反ゼロ）。
- 2026-04-27: M3 #8 landed（feat/m3-tree-flat-views）。**スコープ拡張**: 元 #8 の tree + flat に加えて、ユーザー要望で表示モード（list / grid）切替も同梱。3 commit 構成: (1) `core::search::views::View` に `display: ViewDisplay { List, Grid }` 追加（serde `#[serde(default)]` で欠落キー → List、forward-compat: 旧 views.toml は無改修で読める）、`progest view save --display list|grid` CLI 拡張（`ViewDisplayArg` clap value enum）、views golden 2 件追加（display round-trip + 欠落キー → List）+ CLI smoke `view_save_persists_display_grid`。(2) `progest-tauri` に IPC 4 追加: `view_list()`（NotFound → empty doc 正規化）/ `view_save(view: View)` / `view_delete(id)` / `files_list_dir(path)`（`std::fs::read_dir` + `IgnoreRules`（builtin + `.progest/ignore`）で dotfile / ignore 一致 / `.progest/` を弾き、`std::fs::canonicalize` + `starts_with` で `..` エスケープガード、ファイルは `Index::get_file_by_path` + `rich_rows` で tags / violations / custom_fields 補完、未 reconcile も基本 entry を返してツリーが index ではなく FS 真実を反映）。`build_dir_entry` に extract して clippy `too_many_lines` 100 行制限を満たす。(3) `app/` 大幅再構成: `@/lib/project-context.tsx` で `<ProjectProvider>` を切り出し、project / recent / openPicker / pickRecent / clearRecent / error を Provider 経由で共有（CommandPalette / Welcome / TopBar 全て `useProject()` で取得）。`@/components/tree-view.tsx`（lazy load: 展開時に `files_list_dir`、cache は path→`{state, children}` のレコード、ローディング / エラーをインライン表示、dirs first + case-insensitive 名前ソート）。`@/components/flat-view.tsx`（クエリ Input + saved view 用 native `<select>` + list/grid toggle button + Save-as Dialog（id `[a-z0-9_-]{1,64}` バリデーション + 既存 id への上書き警告）+ list/grid 切替で `<HitList>` / `<HitGrid>` を切替、grid タイルは Lucide `FileIcon` 大 + name + kind + violations、200ms debounce 検索）。`@/components/violation-badges.tsx` で `<ViolationBadges>`（フル）+ `<ViolationDots>`（コンパクト）を共有抽出（CommandPalette / FlatView / TreeView 全部から再利用）。`@/components/command-palette.tsx` は `useProject()` 化 + TopBar 撤去（App shell 側に移動）+ `onPickHit` prop でメイン shell の詳細 Dialog にハンドオフ。`App.tsx` は `<ProjectProvider>` ラップ + project の有無で `<MainShell>`（grid layout: TopBar / Tree(260px) + Flat）/ `<Welcome>`（Open + recent 一覧）を切替、`<TreeFileDetail>` で DirEntry → RichSearchHit shape に変換し共通の `<ResultDetailDialog>` を再利用。`mise run check` + `pnpm --filter @progest/app build` green（308 KB / 95 KB gzip）。**`tauri dev` 実機 smoke は未実施**。次セッションで Cmd+K / 検索結果クリック / tree 展開 / saved-view 保存→読み込み round-trip / list↔grid toggle / no-project Welcome 動線 を目視確認。スコープ外で持ち越し: ディレクトリインスペクター + accepts 編集 + placement 専用バッジ色（M3 #9）、サムネ画像（M4）、view ID rename / sort UI / group_by UI / 並び替え（v1.x）。
- 2026-04-27: M3 #7 landed（feat/m3-command-palette）。3 commit 構成: (1) `core::search::history` を新設、`.progest/local/search-history.json`（schema_version 1 + entries[]）/ retention 100 / dedup append / atomic save、`ProjectRoot::search_history_json()` accessor + `SEARCH_HISTORY_FILENAME` const、10 unit test。(2) `progest-tauri` に `app_info` / `search_execute` / `search_history_list` / `search_history_clear` の 4 IPC commands を追加、`AppState`（`Mutex<Option<ProjectContext>>`、`SqliteIndex` を起動時に open し常駐）、起動時 attach は `PROGEST_PROJECT` env → CWD walk → None フォールバック、`SearchResponse.parse_error: { message, column? }` で parse 失敗を構造化、validate warnings は string 配列、custom_fields は serde tagged union (`type` / `value`)、エラーは `no_project:` プレフィックスでフロント分岐、schema.toml は CLI と同形 inline TOML reader。chrono は `clock` feature を tauri 側で有効化。clippy `needless_pass_by_value` は Tauri commands 単位で `#[allow]`。(3) `app/` 側に `@/lib/ipc.ts`（typed wrappers + `IpcError.isNoProject`）、`@/components/command-palette.tsx`（window keydown で Cmd+K / Ctrl+K toggle、shadcn `CommandDialog` + `shouldFilter={false}`、空入力で recent history + 「Clear recent queries」アクション、非空入力は **200 ms debounce** で `search_execute`、結果行に violation バッジ（naming amber / placement sky / sequence violet、HANDOFF §3.6 の placeholder palette 準拠）、status row に project name / loading / warnings / IPC error 集約、parse error はインライン destructive 表示、no-project は専用 empty state）、`@/components/result-detail-dialog.tsx`（クリックで開く詳細 Dialog、path / file_id / name / ext / tags / violations / custom_fields をフラットに、選択時はパレット閉じてモーダル重なり回避）、`App.tsx` を M0 placeholder から `<CommandPalette />` + Cmd+K hint へ差し替え。`pnpm --filter @progest/app typecheck` + `build` + `mise run check` green、`pnpm build` 288 KB / 90 KB gzip。**`tauri dev` での実機目視は未実施**（M3 #8 着手時か別セッションで補完）。tree/flat view、`view.{list,save,delete}` IPC、`accepts.{read,write}` IPC、placement バッジ正式色、ファイル `Reveal in Finder` 等は本 PR スコープ外で M3 #8 / #9 へ持ち越し。
- 2026-04-26: M3 #6 landed（feat/m3-shadcn-init）。`app/` workspace に Tailwind v4 (`tailwindcss` + `@tailwindcss/vite`) を導入、`@/*` import alias を `app/tsconfig.json` (baseUrl + paths) と `app/vite.config.ts` (`resolve.alias`) に配線。`pnpm dlx shadcn@latest init --preset b1D0dy4m --template vite --pointer -c app` を実行し `app/components.json` (style=`radix-mira`、iconLibrary=`lucide`、baseColor=`neutral`、cssVariables=true、aliases は `@/components{,/ui}` / `@/lib{,/utils}` / `@/hooks`) を生成。`src/index.css` は preset b1D0dy4m により `@theme inline` ブロック + light/dark の oklch トークン + Geist Mono + `tw-animate-css` + `html { font-mono }` + buttons の `cursor: pointer`（`--pointer`）で初期化。`Button` / `Dialog` / `Command` をコマンドパレット土台として追加（CLI が `Input` / `Textarea` / `InputGroup` を依存として同時に追加）、`@/lib/utils` (`cn`) を新設。`pnpm --filter @progest/app typecheck` + `build` + `mise run check` 全 green。`App.tsx` は M0 placeholder のまま据え置き、コマンドパレット配線は M3 #7 で別 PR。
- 2026-04-25: 初版作成。M3 kickoff（feat/m3-search-dsl-spec）。`docs/SEARCH_DSL.md` 初版 landed と同 PR で本ドキュメントを起こす。スコープは §5 M3 のリスト + Tauri IPC 同時 + lindera defer 明示。順序は DSL 仕様書 → core::search → FTS5 → CLI → UI、PR 粒度はフェーズ単位 7〜9 PR 想定。M2_HANDOFF はそのまま M2 完了アーカイブとして残す。
- 2026-04-26: M3 #5 landed（feat/m3-cli-search-tag-view）。CLI `progest search` / `progest tag {add,remove,list}` / `progest view {save,delete,list}` の 3 subcommand を全部入りで実装。`Index` trait を `set_search_projection` / `replace_violations` / `set_custom_field` / `rich_rows` の 4 method で拡張、`SqliteIndex::with_connection` を pub 化（executor が SQL を流す seam）、reconcile を 3 upsert 経路すべてで `set_search_projection` 呼ぶように変更（`name`/`ext`/`notes`/`updated_at` を populate、`is_orphan` は v1 では false 固定）、`progest lint` が実行ごとに visited file_id ごとの violations を replace（lint 走った file の状態が DB と同期）。`core::tag`（add/remove/list + 検証）、`core::search::views`（loader/saver/upsert/delete + View / ViewsDocument）、`core::search::execute::project_hits` + `RichSearchHit { file_id, path, name, kind, ext, tags, violations, custom_fields }`（SEARCH_DSL §8.1 と 1:1）を新設。`OutputFormat` に `Copy` を追加（rename/undo の `&format` を `format` に）、`ProjectRoot::views_toml()` accessor + `VIEWS_TOML_FILENAME` const 追加。CLI 8 smoke test（tag add → search by tag、view save → search via view、lint → is:violation 連携など）+ 既存 全 600+ test pass。Tauri IPC 同時実装は本 PR スコープ外で UI フェーズ (M3 #6+) に持ち越し。
- 2026-04-25: M3 #3+#4 landed（feat/m3-search-executor）。`core::index` migration 0002 で `files` に `name`/`ext`/`notes`/`updated_at`/`is_orphan` 列追加 + `custom_fields` / `violations` テーブル + `files_fts` FTS5 virtual table（trigram、UPDATE/DELETE/INSERT トリガで sync）。`core::search::execute` が `PlannedQuery` を SQLite に流して `Vec<SearchHit { file_id, path }>` を返す。`is:violation`/`is:misplaced` は violations テーブル EXISTS、`is:duplicate` は fingerprint self-join、`is:orphan` は `is_orphan` flag に planner を rewrite（旧 `has_*_violation` 案撤回）。`BindValue` の `ToSql` 実装、planner SQL を outer SELECT で wrap して path 投影。22 executor integration test（empty/tag/type/AND/OR/NOT/freetext/FTS triggers/range/is:* 全種/glob/custom int/順序/複合 query）+ 既存 80 = 102 search test pass。reconcile/lint からの書き手は M3 #5 CLI で合流予定。
- 2026-04-25: M3 #2 landed（feat/m3-core-search-parser）。`progest_core::search::{ast, lex, parse, validate, plan}` の 4 段 pure pipeline。二状態字句解析、再帰下降パーサ（OR > AND > NOT）、`Warning` 列挙、`AlwaysFalse` で unknown_key / type_mismatch / kind 値不正 / glob 不正 / datetime 不正 / `created`/`updated` 重複 / `type:` AND 多重 を全部短絡で吸収（parse は通る）、planner が決定的 SQL + `BindValue` を生成。72 unit + 8 golden（§10 worked examples 1:1）= 全 80 テスト pass。chrono を workspace + core に追加（`Z` / `±HH:MM` 両対応、UTC 正規化）。executor + FTS5 + custom_fields テーブルは M3 #3+#4 で合流予定。
