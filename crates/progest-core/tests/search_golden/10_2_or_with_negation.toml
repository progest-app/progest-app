name = "10.2 (tag OR tag) -is:violation"
query = "(tag:wip OR tag:review) -is:violation"

[expected]
sql_contains = [
  " OR ",
  " AND ",
  "NOT ",
  "FROM violations",
  "v.severity IN",
]
text_params = ["wip", "review"]
warning_count = 0
