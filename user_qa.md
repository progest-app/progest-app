- acceptsにviolationにはしないがsuggestさせるモードが欲しい
    - てかこっちがDefault
    - 今の状態は`strict`
- コマンドパレットのバッジの行がふぞろい
- AIの提案に対してApplyAllとDiscardがほしい
- 現在存在するtagを使いまわせるようにしたい
- 特定のディレクトリ下のファイルを`under:`でフィルターできるように
- ファイル、ディレクトリのCRUD実装
    - TreeView上のDnD含むMove Rename Delete Createなど
- DnDでのOut対応(drag-rsは導入したはず)
    - 後述の連番のコンパクトビューではまとめてドラッグアウトできるように
- 命名規則作成ウィンドウ
    - 命名規則の適応もできるように
- `F1`キーでコマンドパレット
- Tagが多い時Treeviewの名前とアイコンが潰れる
    - どっちかと言うとTagが潰れるべき
- ファイルサイズや画像や動画に合わせてもう少し情報を出したい
    - 秒数、解像度、FPSなど
- Filenameでソートしたい
- ListViewのFilenameの表示に対応したい
    - Pathだと長すぎることがある
- スクロールバーでかすぎ
- 更新の度にUIスレッドがブロッキングされているきがする。
- サムネイル作成をバックグラウンドで(ステータスバーに進捗表示) 
- .metaファイルを隠しファイルにできるか?
    - Finderやエクスプローラーで見た時汚い
- 連番のコンパクト表示
    - `seq (10 files)` みたいな。見た目はあとで凝りたい
- コンテキストメニュー拡充
    - 何に対してのコンテキストメニューかをだす
    - CRUDなど基本的な動作ができるように
    - 外部アプリで開くやFinderで開くとかも
    - あとで色々決め切る
- メニュー作成
    - Macはネイティブのメニュー
    - Win/Linuxはshadcnのmenu
- ダブルクリックでファイルを開く(`open` crate?)
- Historyの強化
    - UI上のUndo/Redo
    - History上限
    - Tagなどへの対応
- インポート時に同一拡張子が多く入っているフォルダを提案
- 既存のTagも参考にAI生成させたい
- AIを用いたテンプレート生成
- テンプレートエディタ
- TreeViewホバー時に対象がすごいズレてる
- クエリ入力時に即検索が走るので入力中もたつく
- 検索の精度が悪い 
    - `bg_`まで入れないと`ch_bg_v10`みたいなファイルにマッチしない
    - `bg`でマッチしてほしい
- Zipなどのエクスポート機能。.metaファイルは出さないようにできる
- Rescanコマンド
- Prune機能
- ディレクトリ削除時に中身を消すかstashするか選べる
- seed拡充
- 検索や命名規則パターンのAutoComplete
- Revocation機能。metaファイルなどを削除し、progestの管理から外す
- fontを`antialiased`に
- UIのアイコン類見直し(これは人間がやる) 
- ファイルのアイコンのバリエーション(色や種類)

